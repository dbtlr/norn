files:
  ignore:
    - "target/**"
    - "fixtures/**"
    - ".claude/**"
validate:
  rules:
    - name: docs-page-base
      match:
        path: "docs/**/*.md"
      required_frontmatter:
        - title
        - description
